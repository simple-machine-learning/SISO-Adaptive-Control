# Mechanical overhead crane payload sway with delay

**System class:** Delayed oscillatory mechanical system  

Shows MPC on payload sway with transport delay. It is a useful reference for combining oscillatory history with explicit prediction.

These figures are representative examples and practical starting points, not universal optimum settings. Recheck sampling, training-data coverage, model order, learning parameters, controller penalties, constraints, and spectral-radius diagnostics for each new operating range.

## Files

- `MPC/mechanical_overhead_crane_payload_sway_with_delay__mpc__lnu.png`
- `MPC/mechanical_overhead_crane_payload_sway_with_delay__mpc__lnu_detail.png`
