# HONU MPC result buttons

The MPC Results panel now mirrors the two workflow actions:

1. Physical ODE Plant Simulation
2. HONU MPC

Each action writes to and reloads from its own NPZ file, so running one mode no longer overwrites the displayed result of the other mode. HONU weights and spectral radii remain available for HONU MPC results only.
