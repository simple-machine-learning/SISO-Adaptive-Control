# Unified spectral-radius and weight-axis labels

- MRAC and MPC use the visible label `rho(A_y(k))` without the redundant word `local`.
- Weight y-axes use only bold `w` as the collective vector label; individual indices are not used on the y-axis.
- The same labels are propagated to normal and full-screen views.
