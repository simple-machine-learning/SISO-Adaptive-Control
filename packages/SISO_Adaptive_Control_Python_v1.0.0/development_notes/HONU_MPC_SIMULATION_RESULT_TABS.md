MPC simulation result-tab update
================================

When `Simulate physical ODE plant` is used, only the Closed loop/simulation tab
is shown. HONU weights and Spectral radii are hidden and their result buttons
are disabled because no HONU/BIBS computation is performed.

After `Run sliding-window HONU MPC`, all three result tabs and buttons are
restored automatically.
