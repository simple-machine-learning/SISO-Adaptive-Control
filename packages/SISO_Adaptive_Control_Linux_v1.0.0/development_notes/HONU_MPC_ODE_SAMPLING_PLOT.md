MPC ODE simulation sampling visualization
========================================

- Pure ODE simulation tab is named `ODE simulation`, not `Closed loop`.
- Dense plant output y is plotted as a green line at dt_sim.
- Samples corresponding to dt_MPC are overlaid as blue points.
- Input u is plotted against the dense dt_sim time base.
- MPC closed-loop plots are unchanged.
