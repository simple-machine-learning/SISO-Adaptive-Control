# HONU MPC compact LM selector

- The Plant HONU learning selector now displays `Ridge` and `L-M`.
- The selector width was reduced from 150 px to 78 px to restore compact alignment in the MPC parameter grid.
- Internal method identifiers remain unchanged (`ridge`, `lm`).
