# HONU MPC sample-point size adjustment

The blue `dt MPC` sample markers in the ODE simulation plot are now approximately half the previous size. Their diameter and outline width still scale immediately with `line width [px]`.
