# HONU MPC: PCA rank and full-screen line width

- The post-run MPC log always prints `PCA_rank` as a dedicated concise line, independently of PCA selection mode.
- MPC full-screen graph windows include a `line width [px]` control.
- Full-screen line and sample-marker widths update immediately without changing current zoom ranges.
