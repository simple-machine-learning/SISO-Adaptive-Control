# HONU MPC concise log update

- Removed initial `u` range and `u_MPC_bounds` from the MPC log.
- The post-run log always reports `PCA_rank`, including Variability mode.
- The same line also reports selected PCA components, base-variable count, and HONU feature count.
